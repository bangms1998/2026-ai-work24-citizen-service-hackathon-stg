{
  "name": "work24-googleform-landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build.mjs",
    "release:check": "npm run build && node scripts/release-check.mjs staging",
    "release:check:production": "npm run build && node scripts/release-check.mjs production",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
